section_type: {section_type}
item_index: {item_index}
item_raw_text:
{item_raw_text}

existing parsed fields:
{existing_parsed_fields}

target_roles:
{target_roles}

required JSON schema hint:
{schema_hint}
